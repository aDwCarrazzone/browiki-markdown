# Claude Code / ferramentas locais
.claude/
__pycache__/
*.pyc

# Logs e saidas temporarias
scratchpad/
scratchpad_*.log
scratchpad_*.py
test_out*/

# Intermediario cru gerado por export_browiki.py (a arvore organizada e' derivada dele)
_flat/

# wiki/ é gerado por build_views.py e publicado no repo separado da Wiki do GitHub.
wiki/

# Imagens (1.2 GB) — versionadas separadamente depois (LFS / Release / repo proprio).
# O markdown referencia images/...; baixe-as com: python export_browiki.py markdown
images/
